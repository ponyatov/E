# align
## [[Attributes|Attributes]]

```event
@aligned(u8)
struct { r'u8 g'u8 b'u8 intense:bool }
```
- [[E/struct]]
- `@aligned(u8|u16|u32|u64)` using [[E/Integer|Integer]] types
- `@aligned(n)` arbitrary bytes number
