# Rounding

```evento
fn floor(x`f32) -> f32     // Round down to integer
fn ceil(x`f32) -> f32      // Round up
fn round(x`f32) -> f32     // Nearest integer
fn trunc(x`f32) -> f32     // Drop fractional part
```
