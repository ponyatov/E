# Basic Arithmetic

```evento
fn sqrt(x`f32) -> f32               // Hardware-accelerated if available
fn pow(x`f32, exp`f32) -> f32       // Table/interpolated for soft-only
fn ln(x`f32) -> f32
fn log10(x`f32) -> f32
```
