# Constraints

```evento
fn is_nan(x`f32) -> bool                  // Check for NaN
fn is_inf(x`f32) -> bool                  // Check for ±Inf
fn clamp(x`f32, min`f32, max`f32) -> f32  // Bound checking
```
