# Type Annotations

[[E/E|E]] supports type inference but allows explicit type annotations:

```E
let x: int = 10          // explicitly typed integer
mut name: string = "E"   // mutable string with type annotation
const DEBUG: bool = true // typed boolean constant
```
